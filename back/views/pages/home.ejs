<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualités - Centre de Gym</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>
    <div class="page-container">
        <header>
            <img src="/image/logo.png" alt="">
            <div class="container">
                
                <h1>CMA GYM D'AUBERVILLIERS</h1>
            </div>
        </header>
        
        <div class="content-wrapper">
            <!-- Barre de navigation latérale -->
            <nav class="sidebar">
                <div class="sidebar-content">
                    <div class="sidebar-header">
                        <h3>Navigation</h3>
                    </div>
                    <ul class="sidebar-menu">
                        <li class="sidebar-item active"><a href="#"><span class="icon">🏠</span> Accueil</a></li>
                        <li class="sidebar-item"><a href="#"><span class="icon">📅</span> Événements</a></li>
                        <li class="sidebar-item"><a href="#"><span class="icon">🏆</span> Compétitions</a></li>
                        <li class="sidebar-item"><a href="#"><span class="icon">👨‍🏫</span> Equipes</a></li>
                        <li class="sidebar-item"><a href="#"><span class="icon">📞</span> Contact</a></li>
                    </ul>
                </div>
            </nav>
            
            <!-- Contenu principal à droite -->
            <div class="main-content">
                <main>
                    <div class="container">
                        <section class="news-list">
                            <% actualities.forEach(actuality => { %>
                                <article class="news-item">
                                    <img src="<%= actuality.image %>" alt="<%= actuality.title %>" class="news-image">
                                    <div class="news-content">
                                        <h2 class="news-title"><%= actuality.title %></h2>
                                        <p class="news-description"><%= actuality.description %></p>
                                        <p class="news-date"><%= new Date(actuality.createdAt).toLocaleDateString() %></p>
                                    </div>
                                </article>
                            <% }); %>
                        </section>
                    </div>
                </main>
            </div>
        </div>
        
        <footer>
            <div class="container">
                <p>&copy; 2025 Centre de Gym. Tous droits réservés.</p>
            </div>
        </footer>
    </div>
</body>
</html>