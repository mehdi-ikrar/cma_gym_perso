<!DOCTYPE html>
<html lang="fr">
    <%- include('../partials/head') %>

<body>
    <div class="page-container">
        <%- include('../partials/header') %>
        
        <div class="content-wrapper">
            <!-- Barre de navigation latérale -->
            <%- include('../partials/sidebar') %>
            
            <!-- Contenu principal à droite -->
            <div class="main-content">
                <main>
                    <div class="container">
                        <h2>Nos activités sportives</h2>
                        
                        
                        <div class="activities-container">
                            <% if (activities && activities.length > 0) { %>
                                <% activities.forEach(activity => { %>
                                    <div class="activity-card" data-public="<%= activity.public %>">
                                        <div class="activity-header">
                                            <h3><%= activity.groupe %></h3>
                                        </div>
                                        <div class="activity-body">
                                            <div class="activity-description">
                                                <%= activity.description %>
                                            </div>
                                            <div class="activity-info">
                                                <div class="activity-time">
                                                    <i class="fas fa-clock"></i>
                                                    <%= activity.horraire %>
                                                </div>
                                                <div class="activity-public">
                                                    <i class="fas fa-users"></i>
                                                    <%= activity.public %>
                                                </div>
                                            </div>
                                            <% if(activity.frequence) { %>
                                                <div class="activity-frequency">
                                                    <i class="fas fa-calendar-alt"></i>
                                                    <span>Fréquence:</span> <%= activity.frequence %>
                                                </div>
                                                <% } %>
                                            <!-- Ajout des nouvelles données -->
                                            <div class="activity-details">
                                                <div class="activity-price">
                                                    <i class="fas fa-tag"></i>
                                                    <span>Cotisation:</span> <%= activity.cotisation %>
                                                </div>

                                            </div>
                                        </div>
                                    </div>
                                <% }); %>
                            <% } else { %>
                                <div class="no-activities">
                                    <p>Aucune activité disponible pour le moment.</p>
                                </div>
                            <% } %>
                        </div>
                    </div>
                </main>
            </div>
        </div>
        
        <%- include('../partials/footer') %>
    </div>

</body>
</html>